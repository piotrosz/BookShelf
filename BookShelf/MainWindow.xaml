<Window x:Class="BookShelf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BookShelf" Height="500" Width="600"
        xmlns:local="clr-namespace:BookShelf"
        Icon="images/bookshelf.ico" Name="mainWindow1">
    <Window.Resources>
        <DataTemplate x:Key="HeaderTemplateArrowUp">
            <DockPanel>
                <TextBlock HorizontalAlignment="Center" Text="{Binding}"/>
                <Path  x:Name="arrow"
                       StrokeThickness = "1"
                       Fill = "Gray"
                       Data = "M 5,10 L 15,10 L 10,5 L 5,10"/>
            </DockPanel>
        </DataTemplate>
        <DataTemplate x:Key="HeaderTemplateArrowDown">
            <DockPanel>
                <TextBlock HorizontalAlignment="Center" Text="{Binding}"/>
                <Path   x:Name="arrow"
                        StrokeThickness = "1"
                        Fill = "Gray"
                        Data = "M 5,5 L 10,10 L 15,5 L 5,5"/>
            </DockPanel>
        </DataTemplate>

        <local:BooleanVisiblityConverter x:Key="convVis"/>
        <Style x:Key="ListViewUnSelected" TargetType="{x:Type TextBlock}">
            <Setter Property="Visibility" Value="{Binding Path=IsSelected, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}}, Converter={StaticResource convVis}, ConverterParameter=False}" />
        </Style>
        <Style x:Key="ListViewSelected" TargetType="{x:Type FrameworkElement}">
            <Setter Property="Visibility" Value="{Binding Path=IsSelected, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}}, Converter={StaticResource convVis}, ConverterParameter=True}" />
        </Style>
    </Window.Resources>
<Grid x:Name="grdWrapper">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0" Grid.Row="0">
            <Menu IsMainMenu="True">

                <MenuItem Header="My library">
                    <MenuItem Header="Add new book"></MenuItem>
                    <MenuItem Header="Books"></MenuItem>
                    <MenuItem Header="Authors" Click="authors_Click"></MenuItem>
                    <MenuItem Header="Publishers"></MenuItem>
                    <MenuItem Header="Categories"></MenuItem>

                </MenuItem>
                <MenuItem Header="Lending">
                    <MenuItem Header="New lending"></MenuItem>
                    <MenuItem Header="Lendings"></MenuItem>
                    <MenuItem Header="Borrowers"></MenuItem>
                </MenuItem>

                <MenuItem Header="Other">
                    <MenuItem Header="Export"></MenuItem>
                    <MenuItem Header="About"></MenuItem>
                    <MenuItem Header="Exit"></MenuItem>
                </MenuItem>
            </Menu>
        </DockPanel>

        <Grid x:Name="grdMain" Grid.Column="0" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Row="0">
                <GroupBox Header="Filter">
                    <StackPanel Orientation="Horizontal" >
                        <TextBlock Text="Filter By :" />
                        <ComboBox ItemsSource="{Binding ColumnNames}" x:Name="cmbProperty" />
                        <TextBox x:Name="txtFilter" MinWidth="50" />
                        <Button x:Name="btnFilter" Click="btnFilter_Click" Content="Apply Filter"/>
                        <Button x:Name="btnClear" Click="btnClear_Click" Content="Clear Filter"/>
                    </StackPanel>
                </GroupBox>
                <GroupBox Header="Grouping">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Group By :" />
                        <ComboBox ItemsSource="{Binding ColumnNames}" x:Name="cmbGroups" />
                        <Button x:Name="btnGroup" Click="btnGroup_Click"  Content="Apply Group"/>
                        <Button x:Name="btnClearGr" Click="btnClearGr_Click"  Content="Clear Group"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
            <ListView ItemsSource="{Binding}" x:Name="lvItems" GridViewColumnHeader.Click="ListView_Click" 
                      IsSynchronizedWithCurrentItem="True" Grid.Row="1">
                <ListView.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Title}" />
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ListView.GroupStyle>
                <ListView.View>
                    <GridView AllowsColumnReorder="True">
                        <GridViewColumn Header="Id" DisplayMemberBinding="{Binding Id}" />
                        <GridViewColumn Header="Title">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding Path=Title}" Style="{StaticResource ListViewUnSelected}"/>
                                        <TextBox Text="{Binding Path=Title}" Style="{StaticResource ListViewSelected}" />
                                    </Grid>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Author">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding Path=Author}" Style="{StaticResource ListViewUnSelected}" />
                                        <ComboBox SelectedItem="{Binding Path=Author}" ItemsSource="{Binding ElementName=mainWindow1, Path=Authors}" Style="{StaticResource ListViewSelected}" />
                                    </Grid>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Image">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Image Source="{Binding Path=Image}"/>
                                    </Grid>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
            <StackPanel Orientation="Horizontal" Grid.Row="2">
                <Button Content="&lt;|" Click="btnNavigation_Click" Tag="0" />
                <Button Content="&lt;" Click="btnNavigation_Click" Tag="1" />
                <Button Content="&gt;" Click="btnNavigation_Click" Tag="2" />
                <Button Content="|&gt;" Click="btnNavigation_Click" Tag="3" />
                <Button HorizontalAlignment="Right" x:Name="btnAdd" Content="Add new book" Click="btnAdd_Click" />
                <Button HorizontalAlignment="Right" x:Name="btnEdit" Content="Edit selected" />
            </StackPanel>
        </Grid>

    </Grid>
</Window>
